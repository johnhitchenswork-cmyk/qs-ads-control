# qs-ads-control
Config that drives the QuoteSmith Google Ads campaign via a Google Ads Script.
Edit `config.json` to control the live campaign (status, budget, keywords, negatives, max CPC).
`status: PAUSED` = no spend. Change to `ENABLED` to go live. No secrets in this repo.
